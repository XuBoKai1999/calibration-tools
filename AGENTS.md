# Repository instructions

- 每次送出使用者可見的 final response 時，同步把完全相同的文字存入 `chat/`。
- 檔名使用下一個三位數流水號：`001.txt`、`002.txt`、`003.txt`……
- 每個檔案只保存該次回覆，不合併、改寫或累積舊回覆。
- 不把 commentary、tool output、思考過程或敏感資料額外寫入交接檔。
